//Create A java script object with style.create function and include all the fonts in the app

import {styleSheet} from 'react-native';

export default fonts = styleSheet.create({
  font1: {
    //write css
  },
});
